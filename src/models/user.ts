export interface User {
  id: string
  sync_approval_status?: string
  sync_approval_status_reason?: string
  full_name?: string
  phone_number?: string
  registration_facility_id?: string
  password_digest?: string
}
